# WARNING: TODO: Only group leader can do this
mutation dismissInvite($id: ID!) {
    updateInvite(
        input: { where: { id: $id }, data: { group_leader_dismissed: true } }
    ) {
        invite {
            id
            group_leader_dismissed
        }
    }
}
