mutation dismissInvite($id: ID!) {
    dismissInvite(id: $id) {
        invite {
            id
            group {
                name
            }
            invitee {
                id
                username
            }
            group_leader_dismissed
        }
    }
}
