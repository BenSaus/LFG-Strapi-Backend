mutation removeMember($groupId: ID!, $memberId: ID!) {
    removeMember(groupId: $groupId, memberId: $memberId) {
        group {
            id
            name
            members {
                id
                username
            }
        }
    }
}
