# WARNING: TODO: Only group leader can do this
mutation rejectApplication($id: ID!) {
    rejectApplication(id: $id) {
        application {
            id
            status
            message
        }
    }
}
