# WARNING: TODO: Only group leader can do this
mutation acceptApplication($id: ID!) {
    acceptApplication(id: $id) {
        application {
            id
            status
            message
        }
        group {
            id
            name
        }
    }
}
