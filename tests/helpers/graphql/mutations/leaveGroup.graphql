mutation leaveGroup($id: ID!) {
    leaveGroup(id: $id) {
        group {
            id
            name
            members {
                id
                username
            }
        }
    }
}
