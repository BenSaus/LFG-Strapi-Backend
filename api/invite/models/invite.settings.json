{
  "kind": "collectionType",
  "collectionName": "invites",
  "info": {
    "name": "Invite",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "invitee": {
      "plugin": "users-permissions",
      "model": "user"
    },
    "message": {
      "type": "richtext"
    },
    "group": {
      "model": "group",
      "via": "invites"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "accepted",
        "rejected",
        "undecided"
      ],
      "default": "undecided"
    },
    "group_leader_dismissed": {
      "type": "boolean",
      "default": false
    }
  }
}
