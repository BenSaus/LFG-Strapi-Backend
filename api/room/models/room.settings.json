{
  "kind": "collectionType",
  "collectionName": "rooms",
  "info": {
    "name": "Room",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "name": {
      "type": "string",
      "unique": true,
      "required": true
    },
    "description": {
      "type": "richtext"
    },
    "image": {
      "model": "file",
      "via": "related",
      "allowedTypes": [
        "images"
      ],
      "plugin": "upload",
      "required": false
    },
    "business": {
      "model": "business",
      "via": "rooms"
    },
    "groups_preferred": {
      "collection": "group",
      "via": "preferred_rooms"
    },
    "player_max": {
      "type": "integer"
    }
  }
}
